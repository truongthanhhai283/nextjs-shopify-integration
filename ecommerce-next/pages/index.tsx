export default function Home() {
  const message: string = "Hello Hai Kute";

  let options: AddEventListenerOptions;
  let b: NodeJS.Process;

  const person: Person = {
    name: "Truong Thanh Hai",
  };
  return <>{message}</>;
}
